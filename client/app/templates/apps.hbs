<h2>These are my applications</h2>


<table class="table table-hover">
	<thead>
		<tr>
			<th>Name</th>
		</tr>
	</thead>
	<tbody>
		{{#each model as |app|}}	
		<tr class="app-row">
			<td class="app-name">{{app.name}}</td>
		</tr>
		{{/each}}
	</tbody>
</table>


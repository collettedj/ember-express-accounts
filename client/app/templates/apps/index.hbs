<button class="btn btn-success" {{action 'createApp'}} >Create App</button>

<table class="table table-hover">
	<thead>
		<tr>
			<th>Name</th>
			<th>Description</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		{{#each model as |app|}}	
		{{#app-row tagName='' app=app}}

		{{/app-row}}
		{{/each}}
	</tbody>
</table>
